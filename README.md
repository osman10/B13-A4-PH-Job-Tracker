1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
   (a) getElementById only one unique element can be selected.
   (b) getElementsByClassName many elements can be selected.
   (c) querySelector Returns only the first matching element
   (d) querySelectorAll Returns all matching elements
   
2. How do you create and insert a new element into the DOM?
   (a) I first create an element (b) Add content and finnaly (c) push the element

3. What is Event Bubbling? And how does it work?
  (a) When an event starts from the target element and then propagates to it's parent element and it's parent...until it reaches the document is call event bubbling. Thus it works.

4. What is Event Delegation in JavaScript? Why is it useful?
   (a) Add event to parent element so that I can apply this to the all lower elemtnt.
   (b) It increases code performence insted of writing many events. 
   
6. What is the difference between preventDefault() and stopPropagation() methods?
   (a) Currently I don't know the answer of this questions. I am trying to find out. 
